dist/
*.log
.claude/agent-memory/
testResults.xml
.claude/worktrees/
.superpowers/

# Environment-probe reports. The probe script is tracked; its output is not -
# each report names the machine and the logged-on user.
tools/nmm-env-probe-*.txt

# Capture-run bundles. Same reason: they name the machine, both accounts
# involved, and carry a full session transcript.
NMM-capture-*/
NMM-capture-*.zip
